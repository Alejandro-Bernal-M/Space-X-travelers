import { createSlice } from '@reduxjs/toolkit';

const initialState= [];

const Rockets = createSlice({
  name: 'Rockets',
  initialState,
  reducers: {},
  extraReducers: {},
})


export  default Rockets.reducer ;